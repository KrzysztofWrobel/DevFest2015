<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.gripit.presentation">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="io.gripit.presentation.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.StatisticsActivity"
            android:label="@string/statistics" />
        <activity
            android:name=".ui.activities.SurveyListActivity"
            android:label="@string/survey_list" />
        <activity
            android:name=".ui.activities.GripitActivity"
            android:label="@string/about_gripit_name" />

    </application>

</manifest>
